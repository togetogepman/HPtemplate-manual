# 研究室HPテンプレート GPT支援オンボーディング仕様

- 文書ID: `GPT_ASSISTED_ONBOARDING_SPEC`
- 版: `v0.1-draft`
- 作成日: 2026-08-04
- 更新日: 2026-08-05
- 対象: `HPtemplate` / `HPtemplate-manual` / 専用GPT / ローカルCodex運用
- 状態: 方針更新ドラフト

---

## 1. この文書の目的

本仕様は、GitHubやHTMLに詳しくない研究者が、研究室HPテンプレートを自分用に複製し、必要最小限の情報と写真を用意し、専用GPTとローカルCodexの支援を受けてGitHub Pagesで公開するまでの利用体験を定義する。

設計の中心は次の二点である。

1. HPに掲載する情報を必要最小限にする。
2. 利用者自身が行う作業と判断を必要最小限にする。

利用者へ複数の作り方を選ばせず、初心者向けには一つの正本ルートだけを提示する。

---

## 2. 確定方針

### 2.1 採用する正本ルート

MVPでは次を採用する。

- 研究室HPテンプレートの紹介ページとマニュアルサイトを、作業開始前の案内役とする。
- 利用者はテンプレートから自分用GitHubリポジトリを作成する。
- GitHub Desktopで自分のPCへcloneし、ローカルフォルダを開く。
- 配布物に含まれる標準Excelを、ローカル入力用フォルダへコピーして入力する。
- 写真等の素材も同じローカル入力用領域へ保存する。
- ローカル入力用領域の内容は`.gitignore`でGit管理対象外とする。
- 入力済みExcelだけを専用GPTへアップロードする。
- 専用GPTはExcelの不足、矛盾、表記揺れ、掲載可否を確認し、必要な確認をまとめて行う。
- 専用GPTはローカルCodex向けの引き継ぎ依頼文を生成する。
- Codexはローカルのclone済みリポジトリを対象に実行する。
- CodexはExcelとローカル素材を読み、HPへ反映し、非破壊的な検証後に今回の変更だけをCommit・Pushする。
- 公開前に利用者が事実、掲載許可、画像、連絡先、PC・スマートフォン表示を確認する。
- GitHub Pagesで公開し、公開URLでも再確認する。
- 利用者は無料の共有GPTを利用できる構成を優先する。

### 2.2 使用するツールの役割

| 主体・ツール | 役割 |
|---|---|
| 利用者 | Excel入力、素材保存、事実・権利・表示の最終確認 |
| 紹介ページ・マニュアル | GPTを開く前の説明、準備手順、完了条件、困ったときの案内 |
| GitHub Desktop | clone、ローカルフォルダを開く、リポジトリの所在を分かりやすくする |
| 専用GPT | 入力済みExcelの確認、不足確認、Codex引き継ぎ文の生成 |
| ローカルCodex | HTML等の変更、素材の配置・リネーム、検証、Commit、Push |
| GitHub Pages | 完成した静的HPの公開 |

### 2.3 採用しないもの

MVPでは次を標準ルートに含めない。

- 専用GPTによるGitHubへの直接書き込み
- GPT Actions、独自API、外部サービスへの自動書き込み
- Codexクラウドを使った初期カスタマイズ
- GPTによる一問一答形式での全情報収集
- GPTによる利用者ごとのExcel生成
- 写真やロゴをGPTへアップロードする運用
- GitHub Issueを使った初期カスタマイズの作業管理
- 利用者によるHTML・CSSの直接編集
- 人の確認を介さない自動公開
- 複数の入力方法や実装方法を初心者向け主導線に並べること

---

## 3. 対象ユーザー

### 3.1 主対象

- 研究者個人または研究室のWebサイトを作りたい人
- GitHub、Git、HTMLの経験がほとんどない人
- Excelへ研究室情報を入力できる人
- 掲載内容の事実確認と公開判断を自分で行える人

### 3.2 想定する開始状態

利用者は紹介ページで完成サンプルを見て、自分の研究室でも同様のHPを作りたいと考えている。

GitHubアカウント、GitHub Desktop、ChatGPTデスクトップアプリ、Codexの準備が済んでいなくてもよい。必要な準備は、実際に使う順序でマニュアルが案内する。

### 3.3 対象外

- 公開内容の責任者が存在しない利用
- CMS、認証、決済、データベース、独自APIを必要とするサイト
- 機密研究情報や公開許可前の個人情報を入力する利用
- 人の確認なしで完全自動公開したい利用

---

## 4. UX原則

1. 完成した研究室HPのPC表示とスマートフォン表示を、技術説明より先に見せる。
2. 各ページで次に行う操作を一つに定める。
3. 初心者向けには一つの正本ルートだけを示す。
4. 各工程に開始状態と完了状態を定める。
5. HPに掲載する情報を必要最小限にする。
6. 利用者が入力・操作する量を必要最小限にする。
7. 入力資料にない事実をAIが補完しない。
8. 「未定」「掲載しない」「後で確認」を正式な状態として扱う。
9. 未入力の任意項目を無理に公開せず、不要なセクションは表示しない。
10. GPTへのファイルアップロードは原則として入力済みExcel一つに限定する。
11. 写真やロゴの原本はローカルに保持し、公開対象だけをCodexが配置する。
12. GPTは不足事項を一問ずつ聞かず、原則としてまとめて確認する。
13. 利用者へ技術的原因の診断を求めず、症状と表示内容を伝えさせる。
14. 待機中は、何を処理しているか、次に何を確認するかを示す。
15. 操作画像は原則として一画像一操作とし、操作後の成功状態も説明する。
16. 公開完了には、公開URLでのPC・スマートフォン再確認を含める。
17. 料金、利用上限、製品画面等の変わり得る情報には確認日または更新管理方法を持つ。
18. 公開前には必ず人が停止・確認する。

---

## 5. 利用者へ見せる四段階

内部の細かい処理は仕様として保持するが、利用者には次の四段階だけを示す。

| 段階 | 利用者がすること | 完了した状態 |
|---|---|---|
| 1. 準備する | 自分用リポジトリを作り、GitHub DesktopでPCへcloneする | 自分用フォルダを開ける |
| 2. 情報と写真を入れる | 標準Excelへ入力し、ローカル入力用フォルダへ写真を保存する | Excelと素材が所定の場所にある |
| 3. HPへ反映する | ExcelをGPTへ渡し、生成された依頼文をローカルCodexへ渡す | 変更後のHPをブラウザで確認できる |
| 4. 確認して公開する | 内容・権利・表示を確認し、GitHub Pagesを設定する | 公開URLでHPを確認できる |

進行表示の例:

```text
1 準備する ─ 2 情報と写真を入れる ─ 3 HPへ反映する ─ 4 確認して公開する
```

---

## 6. 全体ユーザージャーニー

```text
紹介ページで完成サンプルを確認する
  ↓
「研究室HPづくりを始める」を押す
  ↓
GitHubアカウントと必要アプリを準備する
  ↓
テンプレートから自分用リポジトリを作る
  ↓
GitHub DesktopでPCへcloneする
  ↓
配布された標準Excelをローカル入力用フォルダへコピーする
  ↓
Excelへ必要最小限の研究室情報を入力する
  ↓
同じローカル入力用領域へ写真等を保存する
  ↓
入力済みExcelだけを専用GPTへアップロードする
  ↓
GPTが不足・矛盾・表記揺れ・掲載可否をまとめて確認する
  ↓
利用者が必要事項へまとめて回答する
  ↓
GPTがローカルCodex向け引き継ぎ依頼文を生成する
  ↓
利用者がclone済みリポジトリをローカルCodexで開く
  ↓
CodexがExcelとローカル素材を読み、HPへ反映する
  ↓
Codexが検証し、今回の変更だけをCommit・Pushする
  ↓
利用者が内容・権利・PC/SP表示を確認する
  ↓
GitHub Pagesで公開する
  ↓
公開URLで再確認する
```

---

## 7. ローカル入力用領域

### 7.1 目的

入力済みExcel、写真、ロゴ等の作業用素材を、公開対象ファイルと分離しながら、利用者とCodexの双方が迷わず参照できる場所へまとめる。

### 7.2 Git管理

- ローカル入力用領域の内容は`.gitignore`で除外する。
- 入力済みExcel、写真原本、確認メモはCommit・Pushしない。
- 空の標準Excelは配布物としてGit管理する。
- 利用者は標準Excelをローカル入力用領域へコピーしてから入力する。
- フォルダ名、標準Excelの配置、コピー方法はExcel設計時に確定する。

構成案:

```text
HPtemplate/
├─ input/
│  ├─ HP_INPUT_TEMPLATE.xlsx       # 配布用。Git管理する
│  └─ local/                       # 内容を.gitignoreで除外する
│     ├─ laboratory-input.xlsx
│     └─ images/
│        ├─ professor.jpg
│        └─ laboratory.jpg
├─ assets/
│  └─ images/                      # HPで公開する画像
├─ index.html
└─ ...
```

### 7.3 Codexからの参照

`.gitignore`はGitの追跡対象から除外する設定であり、ローカルファイルへの読み取り権限を失わせるものではない。

ただし、検索や自動収集が`.gitignore`を尊重して対象外にする場合があるため、Codex引き継ぎ依頼文ではローカル入力用領域のパスを明示し、必要なExcelと画像を直接読むよう指示する。

### 7.4 画像の扱い

- 利用者は写真原本をローカル入力用領域へ保存する。
- 写真はGPTへアップロードしない。
- CodexはExcelの指定と利用者の指示に基づき、採用する画像だけを公開用画像フォルダへコピーする。
- 公開用ファイルのリネーム、差し替え、形式変換、圧縮、トリミングの詳細は別途定義する。
- 元画像はローカル入力用領域に残し、公開リポジトリへ含めない。

---

## 8. Excel入力方式

### 8.1 Excelの位置づけ

Excelは、研究室HPへ掲載する情報をまとめる標準入力様式である。

GPTとの会話を情報収集の正本にはしない。利用者が途中で作業を中断しても、入力済みExcelとローカル素材から再開できるようにする。

### 8.2 基本フロー

1. 配布された標準Excelをローカル入力用領域へコピーする。
2. 利用者が分かる範囲を入力する。
3. 入力済みExcelを専用GPTへ一度アップロードする。
4. GPTが空欄、矛盾、重複、表記揺れ、掲載可否を確認する。
5. GPTが公開に必要な不足だけをまとめて質問する。
6. 利用者がまとめて回答する。
7. GPTが確認結果とCodex引き継ぎ依頼文を出力する。

Excelの再アップロードは原則として要求しない。修正が必要な場合は、利用者がローカルのExcelを修正し、Codexは修正後の実ファイルを読む。

### 8.3 入力情報の最小化

#### 原則入力する項目

- 研究室名または研究者名
- 所属・肩書
- 短い概要
- 研究テーマ
- HP公開用の問い合わせ先
- 公開判断に必要な注意事項

#### 必要な場合だけ入力する項目

- NEWS
- メンバー
- 論文・研究発表
- 経歴、受賞歴、所属学会
- 写真、ロゴ、図
- 外部リンク
- 英語表記
- SEO、OGP等の追加設定

任意情報がない場合は、空欄を埋めるための文章を生成せず、該当セクションを非表示または削除する。

### 8.4 状態の選択肢

- 確認済み
- 要確認
- 未定
- 掲載しない
- 権利確認中

### 8.5 メールアドレス

- Excelには、完成したHPで一般公開する問い合わせ先メールアドレスを入力できる。
- 私用アドレス、非公開の連絡先、パスワード等は入力しない。
- 公開権限を確認する注意書きをExcel内部に記載する。
- この注意書きをマニュアル上で長く説明する必要はない。
- 具体的な文言と入力欄はExcel設計時に確定する。

### 8.6 Excel設計時の候補

シート数は必要最小限とし、利用者へ多数の入力画面を要求しない。候補は次のとおり。

| シート | 主な内容 |
|---|---|
| `はじめに` | コピー方法、入力方法、公開情報だけを記載する注意 |
| `基本情報` | 研究室名、所属、概要、問い合わせ先、外部リンク |
| `掲載内容` | 研究テーマ、NEWS、メンバー、論文等の任意情報 |
| `画像` | 元ファイル名、用途、掲載可否、権利状態、alt候補 |
| `確認事項` | 掲載しない情報、未定事項、その他の希望 |

最終的なシート・列構成は別途設計し、実利用者が迷わないかをUATで確認する。

---

## 9. 専用GPTの役割

### 9.1 GPTを開くタイミング

専用GPTは利用体験の入口にはしない。

利用者が自分用リポジトリをcloneし、標準Excelへ入力した後に初めて開く。GPTを開く前の準備、入力方法、素材保存方法は紹介ページとマニュアルで説明する。

### 9.2 GPTの完了条件

次を出力できた時点で完了とする。

1. 入力内容の確認結果
2. 未確認・未定・掲載しない情報の一覧
3. 利用者が追加確認すべき事項
4. ローカルCodex向け引き継ぎ依頼文

### 9.3 GPTが行うこと

- 入力済みExcelを読み取る。
- テンプレートの掲載項目へ対応付ける。
- 空欄、矛盾、重複、表記揺れを抽出する。
- 公開に必要な不足だけをまとめて質問する。
- 「未定」「掲載しない」「後で確認」を保持する。
- 入力資料にない事実を補完しない。
- 画像ファイル名と用途の対応を確認する。
- Codexが迷わず作業できる依頼文を作成する。
- 次に利用者が行う操作を一つだけ案内する。

### 9.4 GPTが行わないこと

- 全情報を一問一答で収集すること
- 写真やロゴの受領・リネーム
- GitHubへのIssue作成、ファイル変更、Commit、Push
- GitHub Pagesの設定変更
- 入力資料にない研究内容、論文、所属、受賞、人物情報の創作
- 掲載権限の法的保証
- 公開の最終承認
- 外部APIへの書き込み

### 9.5 GPTの最終出力

#### 入力確認結果

| 項目 | 内容 | 状態 | 掲載可否 | 確認メモ |
|---|---|---|---|---|

#### 未解決事項

- 公開に必須で未確認
- 任意で未入力
- 掲載しない
- 権利確認中
- Codexが実装時に処理できるもの

#### Codex引き継ぎ依頼文

最低限、次を含める。

- 対象リポジトリの確認
- 現在のディレクトリ、branch、HEAD、working tree、remoteとの差分確認
- remote利用前のfetch
- 既存未コミット変更の保全
- `AGENTS.md`、`DESIGN.md`、`README.md`、初期カスタマイズ指示の参照
- 入力済みExcelの明示パス
- 画像素材フォルダの明示パス
- `.gitignore`対象ファイルを直接読む指示
- 掲載内容と掲載しない内容
- 入力資料にない事実を追加しない指示
- 公開画像だけを公開用フォルダへコピーする指示
- PC・スマートフォン表示、リンク、画像の検証
- 今回の変更だけのCommit・Push
- 利用者が確認すべき事項の報告

Issue作成は含めない。

---

## 10. ローカルCodexの役割

### 10.1 実行環境

- Codexはローカルで実行する。
- clone済みの自分用リポジトリを作業対象とする。
- Codexクラウドは標準ルートでは使用しない。
- 初心者向けマニュアルには、採用したローカル実行方法だけを掲載する。

### 10.2 作業開始前の確認

Codexは変更前に次を確認する。

- 現在のディレクトリ
- branch
- HEAD
- working tree
- remote
- remoteとの差分
- `AGENTS.md`
- `DESIGN.md`
- `README.md`
- 初期カスタマイズ指示
- `.gitignore`
- 入力済みExcelと画像素材の明示パス

remoteを利用する作業では、原則として最初にfetchする。

既存未コミット変更がある場合や、remoteが自分用リポジトリを指していない場合は、勝手に修正、破棄、stash、reset、checkout、rebase、commitしない。

### 10.3 Codexが行うこと

- Excelから掲載内容を読み取る。
- 明示されたローカル素材を直接読む。
- テンプレートの既存構造とデザインを維持して内容を反映する。
- 不要なセクションを非表示または削除する。
- 公開対象の画像だけを公開用フォルダへコピーする。
- 必要に応じて画像をリネームし、HTMLから参照する。
- alt、リンク、長い文字列、件数境界を確認する。
- PC・スマートフォン表示を確認する。
- 関連する非破壊的テストと静的検査を実行する。
- テスト成功後、今回の変更だけをCommitし、現在のbranchをPushする。
- 実施済み、未実施、成功、失敗、検証不能を区別して報告する。

### 10.4 Codexが行わないこと

- Git管理対象外の入力済みExcelや写真原本をCommitすること
- 入力資料にない事実の補完
- 無関係なリファクタリングや整形
- 許可のない新規branch、PR、mainへのmerge、branch削除、tag作成
- 人の確認前のGitHub Pages公開設定変更

---

## 11. GitHub Issueを使わない方針

初回カスタマイズではGitHub Issueを作成しない。

理由は次のとおり。

- 初回作業を一つのCodex依頼で完了させる方が利用者に分かりやすい。
- Issue計画、Issue作成、Issue単位の往復は無料利用時のトークンと操作を増やす。
- 未完了事項はCodex最終報告とローカルExcelに残せる。
- 初回公開後に独立した追加改修が生じた場合は、その時点でIssue利用を別途検討できる。

---

## 12. 紹介ページとマニュアルサイト

### 12.1 全体方針

紹介ページとマニュアルは、GPTへの誘導ページではなく、完成像を見せ、正本ルートを順番に案内する導入サイトとする。

主メッセージはツール名ではなく利用者の成果を表す。

例:

> **研究室の資料から、公開できるホームページへ。**  
> 標準Excelへ必要な情報を入力し、ChatGPTとCodexの支援でテンプレートへ反映します。最後に内容を確認して公開します。

主CTAの例:

> **研究室HPづくりを始める**

専用GPTへのリンクは、Excel入力完了後の工程に配置する。

### 12.2 `index.html` — 完成像と全体像

役割:

- 自分にもできそうだと判断してもらう。
- 完成サンプルを先に見せる。
- 四段階の全体像を示す。
- 利用者、GPT、Codex、GitHub Desktop、GitHub Pagesの役割を一枚で示す。
- 主CTAを一つにする。
- 「無料」の範囲と利用上限を分けて説明する。

推奨順:

1. 完成サイトのPC・スマートフォン表示
2. 何が作れるか
3. 四段階の全体像
4. 役割分担
5. 必要なもの
6. 料金・利用条件
7. 一つのCTA

### 12.3 `setup.html` — 自分用環境の準備

完了条件:

- 自分用リポジトリができている。
- GitHub DesktopでPCへcloneされている。
- ローカルフォルダを開ける。
- ローカルCodexが作業対象を開ける。

推奨内容:

1. GitHubアカウントを用意する
2. 必要なアプリを用意する
3. `Use this template`で自分用リポジトリを作る
4. GitHub Desktopでcloneする
5. ローカルフォルダを開く
6. 完了チェック

GitHub操作画像は一操作一画像とし、押す対象と操作後の成功状態を説明する。

### 12.4 `edit.html` — 情報入力とHP反映

完了条件:

- 標準Excelへの入力が完了している。
- 写真等がローカル入力用領域にある。
- GPTからCodex引き継ぎ依頼文を得ている。
- Codexによる変更後のHPを確認できる。

推奨内容:

1. 標準Excelをコピーする
2. 必要最小限の情報を入力する
3. 写真をローカル入力用領域へ保存する
4. Excelだけを専用GPTへアップロードする
5. 不足事項へまとめて回答する
6. Codex引き継ぎ依頼文をコピーする
7. ローカルCodexでリポジトリを開く
8. Codexへ依頼文を貼り付ける
9. Codexの最終報告とブラウザ表示を確認する
10. 症状を伝える修正依頼例を示す

### 12.5 `publish.html` — 確認と公開

完了条件:

- 公開前確認が完了している。
- GitHub Pagesの公開URLが発行されている。
- 公開URLでPC・スマートフォン表示を確認している。
- 次回もExcelとローカルCodexから更新できることを理解している。

公開前確認:

- 研究内容
- 氏名、所属、肩書
- 論文・研究発表
- HP公開用問い合わせ先
- 人物写真・ロゴ等の掲載許可
- 未公開情報の混入
- PC・スマートフォン表示
- リンクと画像
- CodexのCommit・Push先

GitHub Pagesの設定完了ではなく、公開URLでの再確認までを公開完了とする。

---

## 13. 待機・エラー・再開

### 13.1 Codex作業中

```text
Codexがファイルを確認・変更しています。
完了すると、変更内容、テスト結果、確認が必要な点が表示されます。
エラーが出た場合は、自分で原因を調べず、表示内容をそのまま伝えてください。
```

### 13.2 GitHub Pages公開待ち

```text
公開処理を受け付けた後、反映まで時間がかかることがあります。
指定された確認先で処理状態を確認し、公開URLが表示された後に別タブで開いてください。
```

具体的な待ち時間を掲載する場合は、GitHub公式の現行情報を確認し、確認日を記載する。

### 13.3 ChatGPTの利用上限またはアップロード失敗

- 入力済みExcelはローカルに残っているため、失われない。
- 画像はアップロードしない。
- 利用可能になった後、同じExcelを再度アップロードして再開する。
- 失敗時にファイルを分割して多数アップロードする運用は案内しない。

### 13.4 エラー時の伝え方

利用者へ原因分析を求めない。

> 「どの画面で」「何をしたら」「何と表示されたか」を、表示文と一緒にChatGPTまたはCodexへ伝えてください。

症状別の例:

- `Use this template`が見つからない
- GitHub Desktopで自分用リポジトリを開けない
- CodexからExcelまたは写真が見えない
- HPを変更したのに表示が変わらない
- GitHub Pagesの公開URLが表示されない
- ChatGPTへExcelを追加できない
- 次に何をすればよいか分からない

### 13.5 再開

会話履歴やCodexの画面を失っても、次から再開できる。

- ローカルの入力済みExcel
- ローカル素材
- GitHub上の最新Commit
- Codexの最終報告を保存している場合はその内容

Issueを再開の前提にはしない。

---

## 14. 専用GPTの構成

### 14.1 Instructionsへ記載するもの

- 役割と完了条件
- Excelを最初に受け取ること
- 一問一答で全項目を収集しないこと
- 不足確認をまとめて行うこと
- 必要最小限の掲載情報を優先すること
- 「未定」「掲載しない」「後で確認」の扱い
- 事実性ルール
- 機密情報ルール
- 入力確認結果の形式
- Codex引き継ぎ依頼文の形式
- GitHubへ書き込まない制約
- 写真をアップロードさせないこと
- 次の操作を一つだけ案内すること

### 14.2 Knowledgeへ登録するもの

- `AGENTS.md`
- `README.md`
- `DESIGN.md`
- 初期カスタマイズ指示
- 公開前チェック仕様
- Excel入力定義
- ローカル入力用領域の定義
- テンプレートの主要構造説明

### 14.3 有効にするCapabilities

- Data Analysis: Excelの読み取りに利用
- Web検索: Excelに記載された公開URL等の確認が必要な場合だけ利用
- ファイルアップロード: 原則としてExcelの受領に利用

### 14.4 使用しないもの

- Apps
- Actions
- 独自API
- 外部サービスへの自動書き込み
- 画像を前提とする入力フロー

### 14.5 Conversation starters

一問一答や複数入力方式を並べず、Excel入力後の開始に限定する。

例:

- `入力済みの研究室HP用Excelを確認してください`
- `Excelの不足と矛盾を確認し、Codexへの依頼文を作ってください`
- `前回の確認結果と修正済みExcelから再開してください`

---

## 15. MVP受入条件

### 15.1 紹介ページ・マニュアル

- 最初の画面だけで、どのような研究室HPを作れるか分かる。
- 完成サイトのPC・スマートフォン表示を確認できる。
- 押すべき主ボタンが一つに見える。
- 四段階の正本ルートが理解できる。
- ChatGPT、Codex、GitHub Desktop、GitHub Pagesの役割を区別できる。
- 各ページに完了条件が表示される。
- 各ページ末尾の次の操作が一つに定まる。
- GitHubの操作画像が一操作一画像になっている。
- 専用GPTへのリンクがExcel入力後の工程に配置されている。
- 待機、エラー、再開の方法が分かる。
- 「無料」の対象と利用上限が分けて説明されている。
- 変わり得る製品画面や条件に確認日または更新方法がある。

### 15.2 Excelとローカル入力

- 空の標準Excelがテンプレートに含まれる。
- 入力済みExcelを保存するローカル入力用領域がある。
- ローカル入力用領域の内容が`.gitignore`で除外される。
- 利用者が標準Excelをコピーして入力できる。
- 写真等の原本を同じ領域へ保存できる。
- Excel内部にHP公開用メールアドレスの注意書きがある。
- 入力項目が必要最小限で、任意情報を無理に要求しない。

### 15.3 GPT

- 入力済みExcelを読み取れる。
- 写真のアップロードを要求しない。
- 全情報を一問一答で収集しない。
- 不足、矛盾、表記揺れ、掲載可否をまとめて確認できる。
- 不明項目を無理に埋めない。
- 入力資料にない事実を生成しない。
- Issue計画を作成しない。
- ローカルCodex向け依頼文を出力できる。

### 15.4 Codex引き継ぎ

- 利用者がCodexへ貼り付ける依頼文は原則一つで済む。
- Codexが作業開始前のGit状態を確認できる。
- `.gitignore`対象のExcelと素材を明示パスから読める。
- 入力済みExcelや写真原本をCommitしない。
- 公開対象だけをHPへ反映できる。
- PC・スマートフォン表示等を検証できる。
- テスト成功後、今回の変更だけをCommit・Pushできる。
- Issueを作らずに初期カスタマイズを完了できる。
- 人の確認が必要な点を最終報告で区別できる。

### 15.5 公開

- 公開前に事実、権利、連絡先、表示を人が確認する。
- GitHub Pagesの待機状態と確認方法が説明されている。
- 公開URLでPC・スマートフォン表示を確認するまで完了扱いにしない。
- 次回も入力済みExcel、ローカル素材、ローカルCodexから更新できる。

---

## 16. 非機能要件

### 16.1 安全性

- APIキー、認証情報、パスワード、未公開研究情報を要求しない。
- 個人情報はサイト掲載に必要な最小範囲に限定する。
- 入力済みExcelと写真原本を公開リポジトリへ含めない。
- 公開用メールアドレス以外の連絡先を要求しない。
- 公開する画像だけをGit管理対象へ移す。

### 16.2 保守性

- GPT InstructionsとKnowledgeの役割を分離する。
- Excel項目、初期カスタマイズ指示、テンプレート項目の対応を維持する。
- マニュアルとGPT内説明を不要に二重管理しない。
- 製品UI画像には確認日または更新手順を持つ。
- ローカル入力用領域の構造を安易に変更しない。

### 16.3 可用性

- GPTが使えない場合でも入力済みExcelと素材はローカルに残る。
- ChatGPTの利用上限後も同じExcelから再開できる。
- 会話やIssueへ依存せず、最新Commitとローカル入力から再開できる。
- 画像アップロード上限がHP作成の阻害要因にならない。

---

## 17. 実装フェーズ

### Phase 1: 仕様確定

- 本仕様のレビュー
- 正本ルートの確定
- 未決事項の整理

### Phase 2: ローカル入力構造

- 配布用Excelの配置
- ローカル入力用フォルダの配置
- `.gitignore`規則
- Codexが明示パスから読めることの検証
- 入力済みExcelと写真原本がCommitされないことの検証

### Phase 3: Excel入力テンプレート

- シートと列の最小化
- メールアドレス注意書き
- 状態選択
- サンプルデータ
- GPT読取テスト
- 非エンジニアUAT

### Phase 4: 専用GPT

- Instructions作成
- Conversation starters設定
- Knowledge登録
- Excel確認テスト
- 不足事項の一括確認テスト
- Codex引き継ぎ依頼文テスト

### Phase 5: Codex引き継ぎ

- ローカル入力用領域の読取
- 公開画像のコピー・リネーム
- 初期カスタマイズ
- 検証
- Commit・Push
- 最終報告

### Phase 6: マニュアルサイト更新

- 完成像中心のHero
- 主CTAの一本化
- 四段階の全体像
- 役割分担図
- 各ページの完了条件
- 一操作一画像
- 待機、エラー、再開導線
- 公開URLでの再確認

### Phase 7: E2Eテスト

- 架空研究室データ
- Excel入力
- ローカル画像保存
- GPT確認
- ローカルCodex実装
- Commit・Push
- GitHub Pages公開
- 公開URL確認

### Phase 8: 限定UAT

- 初心者ユーザーによる実施
- 離脱点、誤解、過剰入力の記録
- Excel、GPT Instructions、マニュアルの修正
- 実測所要時間の確認

---

## 18. 未決事項

1. 専用GPTの正式名称
2. GPTの紹介文
3. 標準Excelの正式名称
4. Excelの最終シート・列構成
5. 配布用Excelとローカル入力用フォルダの正式パス
6. 利用者がExcelをコピーする具体的な操作
7. 画像の命名、差し替え、形式変換、圧縮、トリミングの詳細
8. 画像と掲載位置をExcelでどう対応付けるか
9. Codexへローカル入力用パスを確実に伝える方法
10. GPTが確認結果をExcelへ書き戻すか、会話内の確認表だけにするか
11. 日本語のみか、英語入力もMVP対象にするか
12. Knowledge更新の運用担当と更新頻度
13. GPT Store公開の判断時期
14. 有償サポート導線との関係
15. 製品画面と料金・利用上限の確認頻度

---

## 19. 設計上の判断まとめ

| 論点 | MVP判断 |
|---|---|
| 専用GPT | 採用。ただしExcel入力後に利用する |
| GPTのGitHub直接書き込み | 不採用 |
| GPT Actions / 独自API | 不採用 |
| 情報入力 | 配布済み標準Excelを正本とする |
| 一問一答 | 標準ルートでは不採用 |
| GPTへの画像アップロード | 不採用 |
| GitHub Desktop | 採用 |
| ローカル入力用領域 | 採用し、内容を`.gitignore`する |
| Codex | ローカル実行を正本とする |
| Codexクラウド | 標準ルートでは不採用 |
| Issue | 初期カスタマイズでは不採用 |
| HTML手作業編集 | 非推奨 |
| 掲載情報 | 必要最小限 |
| 利用者作業 | 必要最小限 |
| 公開前の人確認 | 必須 |
| マニュアルの入口 | 完成像と四段階を提示する |
| GPTへの導線 | Excel入力後の工程に配置する |
| 公開完了 | 公開URLでの再確認まで |

---

## 20. 次の作業

1. ローカル入力用フォルダと`.gitignore`の仕様を確定する。
2. 標準Excelのシート・列・注意書きを設計する。
3. Excel入力からCodex引き継ぎまでの専用GPT Instructionsを作成する。
4. ローカルCodexがignore対象のExcelと画像を明示パスから読めることを検証する。
5. 画像のリネーム・差し替え・公開用配置フローを設計する。
6. 四ページのマニュアル構成を本仕様へ合わせて更新する。
7. 架空研究室データでE2Eテストを行う。
8. 初心者ユーザーによる限定UATを行う。
