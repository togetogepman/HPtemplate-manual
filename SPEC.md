# HPtemplate-site 紹介・配布・利用マニュアル UX・運用仕様

- 文書ID: `HPTEMPLATE_SITE_SPEC`
- 版: `v1.0-draft`
- 作成日: 2026-08-04
- 更新日: 2026-08-05
- 対象: `HPtemplate-site` / `HPtemplate` / ローカルCodex運用
- 状態: 方針更新ドラフト

---

## 1. 目的

`HPtemplate-site`を、研究室HPテンプレートの紹介・配布導線と利用マニュアルを一体化した公式サイトとして定義する。GitHubやHTMLに詳しくない研究者が、完成像と利用条件を確認し、テンプレートを自分用に複製し、Excelへ必要最小限の情報を入力し、ローカルCodexで既存テンプレートへ反映して、GitHub Pagesで公開するまでの利用体験を扱う。

設計の中心は次の二点である。

1. HPに掲載する情報を必要最小限にする。
2. 利用者自身が行う入力、判断、AIとの往復を必要最小限にする。

初心者向けには複数の作成方法を並べず、一つの正本ルートだけを示す。

---

## 2. 確定方針

### 2.1 正本ルート

1. 紹介ページで無料であること、特徴、完成サンプルを確認する。
2. GitHubアカウントを用意する。
3. ChatGPTデスクトップアプリとGitHub Desktopをインストールする。
4. テンプレートからPrivateの自分用GitHubリポジトリを作成する。
5. GitHub DesktopでリポジトリをPCへcloneする。
6. 配布済みExcelをローカル入力用フォルダへコピーする。
7. 各シートへ必要情報を入力し、セクションごとの掲載状態を選択する。
8. 写真等の素材をローカル入力用フォルダへ保存する。
9. ChatGPTデスクトップアプリのローカルCodexでclone先を開く。
10. マニュアルにある一文の実行指示をCodexへ渡す。
11. Codexが`AGENTS.md`の初期カスタマイズ規則、Excel、画像、`index.html`を読み、内容を反映する。
12. CodexがPrivateのまま変更、検証、技術的な保存を行う。
13. 利用者が内容、権利、連絡先、PC・スマートフォン表示、GitHub上のファイルと変更履歴を確認する。
14. 人の確認完了後にリポジトリをPublicへ変更し、シークレットウィンドウから閲覧できることを確認する。
15. Public化とは別工程でGitHub Pagesを設定する。
16. Pagesの公開URLをシークレットウィンドウから開き、PC・スマートフォンでも再確認する。

通常のChatGPTによるExcel確認、専用GPT、ChatGPTとGitHubの接続は、初期カスタマイズの標準工程に含めない。入力規則と実行規則をExcelおよび`AGENTS.md`へ固定し、無料利用枠と利用者の操作を節約する。

### 2.2 ツールの役割

| 主体・ツール | 役割 |
|---|---|
| 利用者 | Excel入力、素材保存、掲載判断、事実・権利・表示の最終確認 |
| 紹介ページ・マニュアル | 完成像、無料範囲、準備、入力、実行、公開、トラブル対応の案内 |
| ChatGPTデスクトップアプリ | ローカルCodexを利用する入口 |
| GitHub Desktop | clone、ローカルフォルダを開く、変更状態を確認する |
| Excel | 掲載内容、掲載状態、画像対応、公開判断を保存する入力正本 |
| `AGENTS.md` | 初期カスタマイズの変更範囲、安全条件、セクション処理をCodexへ指示する正本 |
| ローカルCodex | Excelと素材を読み、既存HPへ反映し、検証、Commit、Pushする |
| `sample.html` | 全セクションを保持する完成サンプル兼、後日復元する際の構造参照元 |
| `DESIGN.md` | デザイン変更、新規ページ追加、構造変更等の後日改修で参照する設計正本 |
| GitHub | 初期作業中はPrivateで自分用リポジトリと変更履歴を保存し、人の確認後にPublicへ変更する |
| GitHub Pages | 完成した静的HPの公開 |

マニュアルでは各ツールを一、二文で説明し、詳細は公式ページへ委ねる。画面、URL、料金、提供条件は変わり得るため、確認日または更新方法を管理する。

### 2.3 標準ルートに含めないもの

- 専用GPTの作成・利用
- 通常のChatGPTによるExcel監査とCodex依頼文生成
- ChatGPTとGitHubの接続
- GPT Actions、独自API、外部サービスへの自動書き込み
- Codexクラウドによる初期カスタマイズ
- GPTによる全情報の一問一答収集
- 写真やロゴのChatGPTへのアップロード
- GitHub Issueによる初期カスタマイズ管理
- 利用者によるHTML・CSSの直接編集
- 人の確認を介さない自動公開

通常のChatGPTは、研究室概要の文章整理、表記統一、エラー説明等で困った場合の任意の補助手段とする。

---

## 3. 対象ユーザー

### 3.1 主対象

- 研究者個人または研究室のHPを作りたい人
- GitHub、Git、HTMLの経験がほとんどない人
- Excelへ研究室情報を入力できる人
- 掲載内容の事実確認と公開判断を行える人

### 3.2 開始状態

GitHubアカウント、GitHub Desktop、ChatGPTデスクトップアプリ、Codexが未準備でもよい。必要なものを実際に使う順序で案内する。

### 3.3 対象外

- 公開内容の責任者が存在しない利用
- CMS、認証、決済、データベース、独自APIを必要とするサイト
- 機密研究情報や公開許可前の個人情報を入力する利用
- 人の確認なしで完全自動公開したい利用

---

## 4. UX原則

1. 最初の画面で、研究室向けテンプレートを無料で利用できることを明示する。
2. GitHub PagesとCodexも無料プランの範囲で完了できるよう設計していることを示す。
3. 無料利用の対象、利用上限、条件変更の可能性を近い位置で説明する。
4. 技術説明より先に、完成した研究室HPのPC・スマートフォン表示を見せる。
5. 各ページで次に行う操作を一つに定める。
6. 各工程に開始状態と完了状態を定める。
7. 掲載情報と利用者作業を必要最小限にする。
8. 初期カスタマイズ時にCodexが読むファイルと変更範囲を限定する。
9. 入力資料にない事実をAIが補完しない。
10. 各セクションはExcelの`このセクションを表示する`で`はい`または`いいえ`を選ぶ。論文分類も`表示する`で同じように扱う。
11. 非掲載セクションをコメントアウトや`hidden`で残さず、`index.html`から外す。
12. 全セクションの構造は`sample.html`へ保持し、後日復元可能にする。
13. 写真やロゴの原本はローカルに保持し、公開対象だけをCodexが配置する。
14. 利用者へ技術的原因の診断を求めず、症状と表示内容を伝えさせる。
15. 操作画像は原則として一画像一操作とする。
16. 公開完了には、公開URLでのPC・スマートフォン再確認を含める。
17. 公開前には必ず人が停止・確認する。
18. 初期作業用リポジトリはPrivateで作成し、Codexによる変更、検証、保存もPrivateのまま行う。
19. Public化前にGitHub上のファイルと変更履歴を確認する。
20. Public化とGitHub Pages設定を別工程として扱う。
21. サインイン中の表示だけで一般公開と判断せず、シークレットウィンドウで確認する。
22. 利用者向け本文は平易な日本語を先に置き、専門用語は括弧で補足する。
23. GitHub、GitHub Desktop、Codex、GitHub Pagesの役割は`setup.html`で一度だけ説明する。
24. 利用者へ使わないルートや代替ツールを提示しない。
25. 利用者が決める必要のないbranchと公開フォルダは、テンプレート本体の仕様に基づく固定値を示す。
26. `AGENTS.md`、`.gitignore`等の内部実装を利用者向け説明の中心にしない。
27. `edit.html`はHPへの反映結果、`publish.html`は公開可否の最終確認を扱い、確認内容を重複させない。
28. Public化とGitHub Pagesの違いを、公開操作の前に説明する。
29. 各ページの完了条件は、利用者が自分で確認できる平易な状態で示す。

---

## 5. 利用者へ見せる四段階

| 段階 | 利用者がすること | 完了した状態 |
|---|---|---|
| 1. 準備する | アプリを導入し、Privateの自分用リポジトリを作成してcloneする | Privateの自分用フォルダをCodexで開ける |
| 2. 情報と写真を入れる | Excelへ入力し、写真をローカル入力用フォルダへ保存する | 全シートの掲載状態が選ばれ、必要な情報と素材がある |
| 3. HPへ反映する | Codexでclone先を開き、一文の実行指示を渡す | 変更後のHPをブラウザで確認できる |
| 4. 確認して公開する | Privateのまま内容・権利・表示を確認し、Public化を外部から確認してからGitHub Pagesを設定する | リポジトリとPages URLをシークレットウィンドウで開き、公開URLをPC・スマートフォンで確認できる |

```text
1 準備する ─ 2 情報と写真を入れる ─ 3 HPへ反映する ─ 4 確認して公開する
```

---

## 6. 完成サンプルと利用開始導線

### 6.1 完成サンプル

`sample.html`は、プレースホルダー集ではなく、架空研究室の完成例として構成する。同時に、テンプレートが提供する全セクションを保持する構造参照元とする。

- 一つの架空研究室を想定し、研究室名、研究分野、教員、メンバー、NEWS、研究テーマ、業績を一貫させる。
- ページ上部に「架空の研究室を想定したテンプレート使用例」と明示する。
- 実在の大学・研究室・人物・論文と誤認されない名称を使う。
- 角括弧プレースホルダー、`Example image`、Lorem Ipsumを残さない。
- 「サンプルです」を各所で繰り返さず、一つの注記で明示する。
- 実在しないDOI、受賞、外部プロフィールへリンクしない。
- `sample.html`には提供する全セクションと対応ナビゲーションを残す。
- 初期カスタマイズで`sample.html`を変更しない。

### 6.2 CTAとHero

- 主CTA: `利用方法を見る`
- 副導線: `完成サンプルを見る`

最初の画面だけで、次が分かることを要件とする。

- 研究室向けHPテンプレートである
- LLMを利用して簡単に作成・更新できる
- テンプレートと公開環境を無料で利用できる
- Codexも無料範囲で使えるよう設計している
- 完成後のイメージを確認できる

Heroコピーの具体的文言は別途決める。

---

## 7. Excelと画像の入力場所

### 7.1 構成案

```text
HPtemplate/
├─ input/
│  ├─ laboratory-input.xlsx        # 利用者が直接編集する。Git管理する
│  └─ images/                      # 画像原本。Git管理する
├─ assets/images/user/             # Codexが配置する公開用画像
├─ AGENTS.md
├─ DESIGN.md
├─ index.html                      # 利用者の公開ページ
└─ sample.html                     # 全セクションを保持する完成サンプル
```

### 7.2 Git管理

- `input/laboratory-input.xlsx`と`input/images/`はGit管理対象である。
- commit・pushするとGitHubへ保存される可能性があるため、公開されても問題ない情報・画像だけを入力・保存する。
- 利用者は同梱された`input/laboratory-input.xlsx`をコピーやリネームせず、そのまま編集・保存する。

### 7.3 Codexからの参照

Codexは固定パスの`input/laboratory-input.xlsx`と`input/images/`を入力の正本として読む。入力用Excelや画像原本を別の場所へ移動・再作成しない。

### 7.4 画像

- 利用者は使用する画像原本を`input/images/`へ保存し、Excelにはフォルダのパスではなくファイル名を入力する。
- 固定画像は`10_画像`、研究テーマ画像は`03_研究テーマ`の各行で使用有無、ファイル名、altを指定する。
- メイン画像は必須、研究テーマ画像、プロフィール画像、大学・組織ロゴ、SNS共有画像（OGP）、サイトアイコンは任意とする。
- Codexは指定された画像だけを原本から`assets/images/user/`へコピーする。
- 入力資料にない画像を生成・取得しない。

---

## 8. Excel入力方式

### 8.1 Excelの位置づけ

Excelは、初期カスタマイズで掲載する情報と判断を保存する入力正本である。ChatGPTとの会話を入力正本にしない。

### 8.2 シート構成

原則としてHPのセクションごとにシートを分ける。

| シート | 対応する内容 |
|---|---|
| `00_チェック` | 各シートの入力状態と全体判定 |
| `01_基本情報` | ページタイトル、検索結果の説明、研究室名 |
| `02_NEWS` | 日付、本文、任意のリンクURL |
| `03_研究テーマ` | 見出し、本文、研究テーマ画像、alt |
| `04_研究室のモットー` | 表題と本文 |
| `05_研究室メンバー` | 区分、年度、氏名・所属等 |
| `06_プロフィール` | 肩書、氏名、外部リンク、経歴等 |
| `07_論文・研究発表` | 論文、発表、記事、書籍等の分類と本文 |
| `08_Contact` | 募集・問い合わせ文、メール、住所、Google Map設定 |
| `09_フッター` | コピーライト |
| `10_画像` | 固定画像の使用有無、ファイル名、alt、推奨 |

Codexはシート名、列見出し、判定、実入力を意味ベースで読み、空の予備行をHTMLへ出力しない。

### 8.3 表示設定

各セクション対応シートの先頭に、次の共通項目を置く。

| 項目 | 選択肢 |
|---|---|
| このセクションを表示する | `はい` / `いいえ` |
| セクション名 | 必要な場合だけ変更可能 |
| 判定 | `00_チェック`から確認する |

`基本情報`等、公開に必須のシートは掲載状態を固定してもよい。

### 8.4 セクションキー

Excel、`index.html`、`sample.html`で同じ安定したセクションキーを使用する。

例:

| 表示名 | セクションキー例 |
|---|---|
| 研究テーマ | `research` |
| メンバー | `members` |
| 業績 | `publications` |
| NEWS | `news` |
| アクセス | `access` |

HTMLでは、例えば次のように識別する。

```html
<section id="research" data-section-key="research">
  ...
</section>
```

ナビゲーションにも同じキーを対応付ける。キーの正式一覧はExcel設計時に確定し、後から安易に変更しない。

### 8.5 表示設定ごとの挙動

| 表示設定 | `index.html`での処理 | Codex最終報告 |
|---|---|---|
| `はい` | 対応セクションをExcel内容で更新して残す | 反映済みとして報告 |
| `いいえ` | 対応セクションとナビゲーション項目を`index.html`から外す | 非表示として報告 |

非表示セクションはコメントアウトや`hidden`属性で`index.html`へ残さない。公開HTMLと将来のCodex入力を小さくし、サンプル文や未確定情報を公開ソースへ残さないためである。

### 8.6 メールアドレス

- 完成したHPで一般公開する問い合わせ先メールアドレスだけを入力する。
- 私用アドレス、非公開の連絡先、パスワード等は入力しない。
- 公開権限を確認する注意書きをExcel内部に記載する。

### 8.7 Google Maps

- `アクセス`シートへ、利用者がGoogle Mapsの共有・埋め込み機能で取得した情報を入力する。
- Codexは既存の地図領域の埋め込み先だけを差し替える。
- APIキーの作成、JavaScript APIの導入、住所からの推測は行わない。
- アクセスセクションを掲載しない場合は、地図とナビゲーション項目も`index.html`から外す。

---

## 9. テンプレート内の初期カスタマイズ規則

### 9.1 位置づけ

Codexへ毎回長いプロンプトを生成して渡すのではなく、初期カスタマイズの作業範囲と安全条件をテンプレート内の`AGENTS.md`と`prompts/INITIAL_CUSTOMIZATION.md`へ固定する。

ここでいう一文とは、利用者がCodexへ入力する短い実行指示である。マニュアルで利用者がCodexへ渡す指示は、原則として次の一文で済むようにする。

```text
prompts/INITIAL_CUSTOMIZATION.md に従って初回カスタマイズを実行してください。
```

一文だけで処理を成立させるには、HPtemplate本体側に`input/laboratory-input.xlsx`、`input/images/`、初期カスタマイズ規則、正式なプロンプトが必要である。これらはHPtemplate本体の正本仕様である。

### 9.2 初期カスタマイズ節に記載すること

- 作業開始前にdirectory、branch、HEAD、working tree、remoteとの差分を確認する。
- remoteを利用する場合は原則として最初にfetchする。
- 既存未コミット変更を勝手に変更、破棄、stash、reset、checkout、rebase、commitしない。
- 初期作業では、`AGENTS.md`、`DESIGN.md`、`LICENSE.md`、`input/laboratory-input.xlsx`、`input/images/`、`index.html`、`sample.html`、必要最小限の関連ファイルを読む。
- レイアウト、CSS、配色、フォント、JavaScript、HTMLの基本構造を変更しない。
- 変更対象を文章、リンク、画像、alt、メタ情報、Google Maps埋め込み、セクション表示設定に限定する。
- セクションキーでExcel、`index.html`、`sample.html`の対応を判断する。
- Excelで「このセクションを表示する」が`いいえ`のセクションは、対応ナビゲーションとともに`index.html`から外す。
- 非表示セクションをコメントアウトまたは`hidden`で残さない。
- `sample.html`は変更せず、全セクションの参照元として保持する。
- 入力資料にない事実を追加しない。
- `input/laboratory-input.xlsx`と`input/images/`はGit管理対象なので、公開されても問題ない情報・画像だけを保存する。
- 指定された公開画像だけを原本を保持したまま`assets/images/user/`へコピーする。
- 関連する非破壊的な表示確認と静的検査を行う。
- テスト成功後、今回の変更だけをCommitし、現在のbranchへPushする。
- 実施済み、未実施、成功、失敗、検証不能、未決事項を区別して報告する。

### 9.3 後日改修との分離

`DESIGN.md`および詳細な`AGENTS.md`の一般規則は削除しない。

- 初期カスタマイズ: 既存デザインを変更せず、初期カスタマイズ節だけを主に使用する。
- デザイン変更、新規ページ追加、構造変更: `DESIGN.md`と関連規則を参照する。
- 非掲載セクションの復元: `sample.html`から同じセクションキーのセクションとナビゲーションを`index.html`へ戻し、Excel内容を反映する。
- 復元時に配置やレスポンシブ挙動の調整が必要な場合だけ`DESIGN.md`を参照する。

---

## 10. `sample.html`とセクション復元

### 10.1 `sample.html`の役割

- 架空研究室の完成例を利用者へ見せる。
- テンプレートが提供する全セクションを保持する。
- 各セクションとナビゲーションに、`index.html`と共通のセクションキーを持たせる。
- 初期カスタマイズでは変更しない。

### 10.2 後日復元

後から非掲載セクションを追加する場合は、次の流れとする。

1. Excelの対象シートの`このセクションを表示する`を`はい`へ変更し、内容を入力する。
2. Codexへ対象セクションキーを示す。
3. Codexが`sample.html`から同じキーのセクションとナビゲーションを`index.html`へ戻す。
4. Excelの内容と画像を反映する。
5. 表示、リンク、レスポンシブ挙動を確認する。
6. 今回の変更だけをCommit・Pushする。

非掲載時に構造を別ファイルへ新規退避する処理は行わない。`sample.html`に全セクションが保持されているため、初期作業時の追加ファイル生成とトークン消費を避ける。

---

## 11. ローカルCodexの役割

### 11.1 初期作業で読むもの

- `AGENTS.md`の初期カスタマイズ規則
- `input/laboratory-input.xlsx`
- `input/images/`
- `index.html`
- `sample.html`
- 変更に直接必要な最小限のファイル

`sample.html`は完成見本・回帰試験用であり、入力元にしない。`10_画像`と`03_研究テーマ`の画像指定を読み、メイン画像が必須であること、その他画像が任意であることを確認する。

### 11.2 行うこと

- Excelの掲載内容と表示設定を読む。
- 文章、リンク、画像、alt、メタ情報、地図埋め込みを差し替える。
- 非表示セクションと対応ナビゲーションを`index.html`から外す。
- 公開画像だけを公開用フォルダへコピーする。
- PC・スマートフォン表示、リンク、画像、長い文字列、件数境界を確認する。
- 関連する非破壊的テストと静的検査を行う。
- テスト成功後、今回の変更だけをCommit・Pushする。

### 11.3 行わないこと

- `sample.html`の変更
- `DESIGN.md`の変更
- デザイン、レイアウト、CSS、JavaScript、基本構造の改善
- 入力済みExcelや写真原本のCommit
- 入力資料にない事実の補完
- 無関係なリファクタリングや整形
- 許可のないbranch、PR、merge、branch削除、tag作成
- 人の確認前のGitHub Pages公開設定変更

---

## 12. GitHub Issueを使わない方針

初回カスタマイズではGitHub Issueを作成しない。

- 一つのCodex依頼で完了させる方が利用者に分かりやすい。
- Issue計画と往復は無料利用枠と操作を増やす。
- 未完了事項はCodex最終報告とローカルExcelに残せる。
- 初回公開後の独立した改修では必要に応じてIssueを検討する。

---

## 13. 紹介ページとマニュアルサイト

4ページの役割は次のとおりとする。

| ページ | 役割 | レイアウト |
|---|---|---|
| `index.html` | テンプレートの紹介、完成サンプル、4ステップの利用案内と詳細マニュアルへの入口 | `HPtemplate`本体の実際の構造と資産を使用する紹介ページ |
| `setup.html` | アカウント、アプリ、自分用リポジトリ、cloneの準備 | `DESIGN.md`に従うマニュアル専用レイアウト |
| `edit.html` | Excel入力、写真保存、ローカルCodexによる反映 | `DESIGN.md`に従うマニュアル専用レイアウト |
| `publish.html` | 人による確認、GitHub Pages、公開URLでの再確認 | `DESIGN.md`に従うマニュアル専用レイアウト |

紹介ページは`HPtemplate`本体の構成を崩さず、実際の`body#top`、`header#header`、`nav#g-nav`、`.main-contents`、セクションdata属性、CSS、JavaScript、クラス、ナビゲーション生成、レスポンシブ挙動を使用する。HPtemplate風に似せた独自紹介ページは採用しない。

マニュアル3ページは`HPtemplate`本体のデザイン、HTML構造、CSS、JavaScriptを使用しない。再整理前のマニュアルデザインを基礎とし、`DESIGN.md`に従って、完了条件、進捗、番号付き手順、注意、Tips、コード例を読みやすくする。

紹介ページとマニュアルでは役割を明確に分離し、header、navigation、CSSクラス、CSSファイル、JavaScript、ページ幅、セクション構造を無理に共有しない。共通化はサイト名称、相互リンク、copyright、文章表現、アクセシビリティ基準等の最小範囲に限る。

### 13.1 `index.html`

- `HPtemplate`本体の実際のテンプレート構造を使用している。
- 独自LP用のheader、navigation、カード構造へ置き換えない。
- 紹介ページ用資産をマニュアル用資産から分離している。
- 最初の画面で研究室向け無料テンプレートだと分かる。
- LLMで簡単に作成・更新できると分かる。
- 完成サイトのPC・スマートフォン表示を見せる。
- 主CTAを`利用方法を見る`とする。
- 副導線を`完成サンプルを見る`とする。
- 四段階の全体像を示す。
- GitHub Desktop、Excel、Codex、GitHub Pagesの役割を簡潔に示す。
- 無料範囲、利用上限、条件変更の可能性を説明する。
- 最終セクションを含め、左ナビゲーションの現在地表示と`aria-current="location"`が正しく更新される。
- ページ最下部では最後の「研究支援・個別開発」を現在地として扱い、上へ戻ると対応セクションへ現在地が戻る。
- 紹介ページ左上に内容を持たない青い四角を表示しない。
- 紹介ページ固有のCSS調整は`assets/template/css/site.css`に置く。
- 無料範囲は、研究室HPテンプレートの初期構築、Codexによる文章・画像・軽微な更新、GitHub Pagesでの公開、マニュアルに従ったセルフサービスとする。
- 有償範囲は、研究プロジェクト・組織向けWebサイト、研究支援ツールの短期試作、研究ソフトウェア・システム開発の3区分とする。
- GitHub Pages開設サポート、初期入力代行、更新代行、テンプレート操作代行、公開前チェックだけの小規模サービスは掲載しない。
- 価格、具体的な納期、「3日開発」という社内名称、予算消化、作業時間、固定価格、契約条件は紹介ページへ掲載しない。

### 13.2 `setup.html`

利用者向け見出しは「このページが終わると」とし、次の状態を平易に示す:

- GitHubアカウントがある。
- ChatGPTデスクトップアプリにサインインできる。
- GitHub DesktopでGitHubへサインインできる。
- 自分用リポジトリがPrivateで作成されている。
- 自分専用コピーがパソコンへ保存され、CodexでHPフォルダを開ける。

ChatGPTとGitHubの接続手順は掲載しない。

初期作業中は一般公開されず、人の確認後の公開工程で初めてPublicへ変更することを説明する。Privateでもパスワード、APIキー、認証情報、公開許可前の不要な個人情報、Excel原本、写真原本、確認用メモをGitHubへ保存させない。

`HPtemplate`本体のクラス、研究室向けセクション、ナビゲーション生成JavaScriptを使用しない。

### 13.3 `edit.html`

利用者向け見出しは「このページが終わると」とし、次の状態を平易に示す:

- Excelの各シートに必要情報と掲載状態が入力されている。
- 写真等がローカル入力用領域にある。
- Codexへ指示文を貼り付け、変更後のHPと未確認事項を確認できる。

説明する手順:

1. 同梱されたExcelを直接編集して保存する。
2. 各シートで表示設定を選ぶ。
3. 使用する画像を`input/images/`へ保存する。
4. Codexでclone先を開く。
5. 一文の実行指示を渡す。
6. CodexがPrivateの自分用リポジトリへ保存した後、変更内容、テスト成功、保存完了、未確認事項を確認する。
7. 修正時は原因ではなく症状を伝える。

利用者へCommit・Pushを主要操作として求めない。利用者向け本文では、Excelの場所、画像の場所、正式なCodex依頼文を平易に案内し、`AGENTS.md`等の内部実装の詳細を中心にしない。

`HPtemplate`本体のデザインを使用せず、マニュアル専用CSSとコピー機能だけを使用する。

### 13.4 `publish.html`

利用者向け見出しは「このページが終わると」とする。Privateの状態で、研究内容、氏名・所属、論文、問い合わせ先、画像権利、未公開情報、PC・スマートフォン表示、リンク、画像、GitHub上のファイルと履歴を、内容・公開許可・GitHub上のファイル・表示の4分類で人が最終確認する。

確認完了後にのみPublicへ変更する。リポジトリがPublic表示になり、そのURLをシークレットウィンドウから閲覧できることを確認してから、別工程としてGitHub Pagesを設定する。

操作前に、Public化はGitHub上のファイルを誰でも見られる状態にする工程、GitHub PagesはそのファイルをWebサイトとして表示する工程だと説明する。`HPtemplate`本体の標準設定に基づき、Branchは`main`、Folderは`/(root)`へ固定する。

GitHub Pagesの設定保存だけでは完了としない。Pages URLが表示され、シークレットウィンドウから開けることを確認し、公開URLでPC・スマートフォン表示、画像、内部リンク、外部リンクを再確認した時点を公開完了とする。

`HPtemplate`本体のデザインを使用せず、マニュアル専用レイアウトを使用する。

### 13.5 Tips

- 通常のChatGPTは、文章作成、表記統一、エラー説明等で困った場合に任意利用できる。
- Claude Code、Gemini CLI等でも同じExcelとローカル素材を応用できる場合がある。
- 標準マニュアル、テスト、UAT、サポート対象はローカルCodexとする。
- 代替ツールを初心者向け主導線へ混在させない。

---

## 14. 待機・エラー・再開

### 14.1 Codex作業中

```text
Codexがファイルを確認・変更しています。
完了すると、変更内容、テスト結果、確認が必要な点が表示されます。
エラーが出た場合は、自分で原因を調べず、表示内容をそのまま伝えてください。
```

### 14.2 GitHub Pages公開待ち

公開反映には時間がかかる場合があること、確認先、公開URLが表示された後の再確認方法を案内する。具体的な待ち時間を掲載する場合は公式情報を再確認する。

### 14.3 エラー時の伝え方

> 「どの画面で」「何をしたら」「何と表示されたか」を、表示文と一緒にCodexまたは任意でChatGPTへ伝えてください。

### 14.4 再開

会話履歴やCodex画面を失っても、次から再開できる。

- ローカルの入力済みExcel
- ローカル素材
- GitHub上の最新Commit
- `sample.html`に保持された全セクション
- Codexの最終報告を保存している場合はその内容

---

## 15. MVP受入条件

### 15.1 紹介ページ・マニュアル

- 最初の画面で研究室向け無料テンプレートだと分かる。
- LLMで簡単に作成・更新できると分かる。
- 無料範囲、利用上限、条件変更の可能性が説明されている。
- 完成サンプルが一貫した架空研究室で、架空だと明示されている。
- `sample.html`に全セクションが保持されている。
- 主CTAが`利用方法を見る`である。
- 各ツールの簡易説明と公式リンクがある。
- ChatGPTとGitHubの接続を要求しない。
- 各ページに完了条件がある。
- GitHub操作画像が一操作一画像である。
- 通常のChatGPTや代替コーディングツールはTipsに留まる。

### 15.2 Excel・画像入力

- セクションごとに入力シートが分かれている。
- 各任意セクションで`このセクションを表示する`を`はい`または`いいえ`にできる。
- 固定画像は`10_画像`、研究テーマ画像は`03_研究テーマ`で指定する。
- `input/laboratory-input.xlsx`と`input/images/`を入力正本として扱う。
- 公開用画像は`assets/images/user/`へ配置し、入力原本を保持する。
- Excel内部に公開用メールアドレスの注意書きがある。
- Google Mapsの入力が単純な埋め込み差し替えに限定されている。

### 15.3 `AGENTS.md`・Codex

- 初期カスタマイズ規則が`AGENTS.md`に定義されている。
- 利用者のCodex入力が原則一文で済む。
- 正式な依頼文が`prompts/INITIAL_CUSTOMIZATION.md`に定義されている。
- `いいえ`にしたセクションがナビゲーションとともに`index.html`から外れる。
- 非掲載セクションがコメントアウトまたは`hidden`で残らない。
- `sample.html`が変更されない。
- `input/laboratory-input.xlsx`と`input/images/`を明示パスから読める。
- Excelと画像原本は公開されても問題ない情報・画像だけを保存する。
- デザイン、CSS、JavaScript、基本構造を変更しない。
- テスト成功後、今回の変更だけをCommit・Pushできる。
- Issueを作らずに初期カスタマイズを完了できる。

### 15.4 復元・公開

- 非掲載セクションを、同じセクションキーに基づいて`sample.html`から復元できる。
- 復元時もExcel内容を反映し、表示確認できる。
- 公開前に事実、権利、連絡先、表示を人が確認する。
- 初期作業用リポジトリがPrivateで作成され、人の確認完了後にPublicへ変更される。
- Public化とGitHub Pages設定が別工程として説明されている。
- リポジトリとPages URLをシークレットウィンドウから閲覧できる。
- 公開URLでPC・スマートフォン表示、画像、リンクを確認するまで完了扱いにしない。

---

## 16. 非機能要件

### 16.1 安全性

- APIキー、認証情報、パスワード、未公開研究情報を要求しない。
- 個人情報はサイト掲載に必要な最小範囲に限定する。
- 入力済みExcelと写真原本を公開リポジトリへ含めない。
- Privateリポジトリにもパスワード、APIキー、認証情報、公開許可前の不要な個人情報、Excel原本、写真原本、確認用メモを保存しない。
- 公開用メールアドレス以外の連絡先を要求しない。
- Google Maps APIキーを使用しない。

### 16.2 トークン・利用枠

- 専用GPTとChatGPTによる事前監査を標準工程から外す。
- 初期カスタマイズでCodexが読むファイルを限定する。
- `AGENTS.md`へ規則を固定し、毎回長い依頼文を生成しない。
- 非掲載セクションを`index.html`から外し、後日の通常更新で読む公開HTMLを小さくする。
- 初回作業を一つのCodex依頼で完了させる。

### 16.3 保守性

- `DESIGN.md`と一般的な`AGENTS.md`規則は後日改修のため維持する。
- セクションキーをExcel、`index.html`、`sample.html`で共通化する。
- `sample.html`を全セクションの構造参照元として維持する。
- Excel項目とHTML項目の対応を安易に変更しない。
- 製品UI、URL、料金、利用上限には確認日または更新方法を持つ。

---

## 17. 実装フェーズ

1. 本仕様のレビューと正本ルート確定
2. `AGENTS.md`の初期カスタマイズ規則設計
3. セクションキーの正式一覧とHTMLへの付与
4. `sample.html`の全セクション保持と架空研究室化
5. `input/laboratory-input.xlsx`と`input/images/`の導線・画像指定の検証
6. Excelのシート、表示設定、列、注意書き設計
7. 初期カスタマイズの一文実行テスト
8. 非表示セクションの除外と後日復元テスト
9. 画像配置、Google Maps、表示確認、Commit・Pushテスト
10. 四ページのマニュアル更新
11. 架空研究室データによるE2Eテスト
12. 初心者ユーザーによる限定UAT

---

## 18. 未決事項

1. 紹介ページの正式なHeroコピー
2. 完成サンプルの架空研究室設定と掲載内容
3. 現行Excelの入力ルールとシート構成の維持
4. セクションキーの正式一覧
5. 画像の命名、対応付け、変換、圧縮、トリミング
6. Google Maps埋め込み情報の具体的な入力形式
7. `AGENTS.md`の初期カスタマイズ節の最終文面
8. 非表示セクションを外した際のナビゲーション区切り等の処理
9. 日本語以外をMVP対象にするか
10. 製品画面、料金、利用上限の更新担当と確認頻度
11. 有償サポート導線との関係

---

## 19. 設計判断まとめ

| 論点 | MVP判断 |
|---|---|
| 情報入力 | セクション別Excelを正本とする |
| 専用GPT | 不採用 |
| 通常のChatGPT | 標準工程から外し、任意Tipsとする |
| ChatGPTとGitHubの接続 | 不採用 |
| Codex依頼 | `prompts/INITIAL_CUSTOMIZATION.md`へ規則を固定し、利用者は一文だけ入力する |
| GitHub Desktop | 採用 |
| Excel・画像入力 | `input/laboratory-input.xlsx`と`input/images/`をGit管理する |
| Codex | ローカル実行を正本とする |
| Codexクラウド | 標準ルートでは不採用 |
| `DESIGN.md` | 維持し、初期カスタマイズでも現行仕様として確認する |
| `sample.html` | 全セクションを保持する完成例兼復元元 |
| 非表示セクション | `index.html`から外し、コメントアウトや`hidden`で残さない |
| セクション復元 | `sample.html`から同じキーの箇所を戻す |
| Issue | 初期カスタマイズでは不採用 |
| Google Maps | 既存埋め込み先の差し替えだけ行う |
| 公開前確認 | 必須 |
| 初期リポジトリ | Privateで作成し、Codexの変更・検証・保存もPrivateのまま行う |
| Public化 | 人の内容・権利・表示確認後に行い、シークレットウィンドウで確認する |
| Pages設定 | Public化とは別工程としてPublic確認後に行う |
| 公開完了 | Pages URLをシークレットウィンドウで開き、公開URLをPC・スマートフォンで再確認するまで |

---

## 20. 次の作業

1. セクションキーの正式一覧を決める。
2. `AGENTS.md`の初期カスタマイズ規則を作成する。
3. `index.html`と`sample.html`へ共通セクションキーを付与する。
4. 現行Excelのシート、表示設定、列、注意書きを維持する。
5. `input/laboratory-input.xlsx`と`input/images/`の案内を検証する。
6. 非表示セクションの除外と`sample.html`からの復元を検証する。
7. 画像差し替えとGoogle Maps埋め込みの規則を確定する。
8. 一文のCodex実行指示で初期カスタマイズが完了するか検証する。
9. マニュアルのChatGPT接続・専用GPT導線を削除し、Excel・Codex導線へ更新する。
10. 架空研究室データでE2Eテストを行う。
11. 初心者ユーザーによる限定UATを行う。
