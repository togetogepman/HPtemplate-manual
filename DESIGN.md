# HPtemplate-manual デザイン方針

## 目的

研究室HPテンプレートを初めて扱う人が、ChatGPTで掲載内容を整理し、Codexへ編集を依頼して、GitHubで公開・更新する手順を迷わず確認できるマニュアルサイトにする。HTMLの直接編集は補足として扱う。

## 基本色

白と淡いグレーを背景に使い、本文は濃いグレーとする。青はリンク、現在地、要点などに限定して使う。

## 文字と余白

日本語のシステムフォントを優先し、本文は16px相当、行間は広めにする。読み物の横幅を抑え、見出し・本文・情報のまとまりの間に十分な余白を設ける。

## 情報設計とナビゲーション

正本ページは`index.html`、`setup.html`、`edit.html`、`publish.html`とする。初回利用者向けの一本道を正本とし、`準備 → 編集 → 公開`の順で進める。技術名による並列ナビゲーションにはせず、利用者の行動を示す見出しとリンクを使う。全ページで「ホーム / 1. 準備 / 2. 編集 / 3. 公開」のナビゲーションを共通化し、現在ページは文字色、下線、HTMLの属性で明示する。

各工程には完了条件を表示し、初回利用者用の次工程ボタンを最も目立たせる。途中再開リンクと前工程リンクは補助導線として扱い、主ボタンより強調しない。

ChatGPTはGitHub上の自分用リポジトリを参照し、テンプレートと研究室資料を照合して、Codexへ貼り付ける完成済み依頼文を作る。Codexはclone済みのローカルリポジトリを変更する。この2種類の接続を混同しない。

## 手順、注意、コード例

手順は番号と見出しで順番を示す。注意は本文と区別できる控えめな強調枠、コード例は横幅からはみ出さない等幅表示を基本とし、必要になった段階で追加する。

## レスポンシブ

PCでは読みやすい最大幅を持つ中央配置とし、スマートフォンでは1列表示に切り替える。リンクの操作領域を確保し、長い日本語やコードでも横スクロールを発生させない。

## HPtemplateとの関係

`HPtemplate`と同一のデザインにはせず、白と淡いグレー、限定的な青、読みやすい日本語、十分な余白、控えめな装飾というテイストだけを合わせる。

操作画像は、文章だけでは選択対象を特定しにくいGitHub操作に限定する。
