# HPtemplate-site デザイン方針

## 目的

`HPtemplate-site`には、役割も実装も異なる二つのデザインがある。紹介・配布ページは配布対象の`HPtemplate`本体を実際に利用し、マニュアル3ページは研究室HPに見えない手順書専用デザインを利用する。

両者を一つの共通デザインシステムへ統合しない。

## 1. 紹介・配布ページ

対象：`index.html`

### 実装基準

- `HPtemplate`本体の実際のHTML構造、CSS、JavaScript、クラス、data属性、挙動を基礎にする
- `body#top`、`header#header`、`nav#g-nav`、`main > .main-contents`の基本構造を維持する
- セクションの`data-accordion`、`data-class`、`data-title`、`scroll-point`を維持する
- JavaScriptによるナビゲーション自動生成、現在地表示、スマートフォンメニュー、必要なセクションの「もっと見る」を利用する
- 左ナビゲーションは最終セクションを含む現在地を表示し、色だけでなくリンクの`aria-current="location"`も更新する
- 本体の見出し、余白、交互背景、研究室向けセクション、レスポンシブ挙動を利用する
- 紹介内容だけを、特徴、完成サンプル、利用方法、配布導線、FAQ、既存サポート向けに置き換える
- 紹介ページのヘッダーでは、内容を持たない青い四角を表示しない
- PCヘッダーと左ナビゲーションは本文を圧迫しないコンパクトな高さ・幅とし、サイト名には日本語・英語とも判読できる文字サイズを確保する
- 黒い連番は順序や件数を示す必要がある場所だけに使用し、現在は「3つの特徴」の3項目だけに表示する
- FAQは各質問の`details`による開閉だけを使用し、外側の「もっと見る」は設けず、すべての質問を最初から表示する
- Supportは従来ページにある3つの有償サービス内容と、アカデメイア公式Contactへの導線を保持する

### 不採用

- `HPtemplate`に色や装飾だけを似せた独自LP
- カード中心の独自ランディングページ構造
- 本体と異なる簡略ヘッダー・ナビゲーション
- 本体JavaScriptの挙動を模倣した別実装

紹介ページ用資産は`assets/template/`に配置し、マニュアル資産から独立させる。紹介ページ固有の調整は`assets/template/css/site.css`へ置き、HPtemplate本体由来の`assets/template/css/style.css`へ混在させない。コピー元の`HPtemplate`は変更せず、このリポジトリ内だけで参照を完結させる。

## 2. マニュアルページ

対象：`setup.html`、`edit.html`、`publish.html`

### 実装基準

- `HPtemplate`本体のデザインを使用しない
- 再整理前のマニュアルデザインを基礎とする
- 白と淡いグレー、限定的な青、濃いグレーの本文を使う
- 日本語システムフォント、約50remの中央本文幅、広めの行間を使う
- マニュアル専用の文字だけのヘッダーと、「ホーム／準備／入力・反映／公開」のナビゲーションを使う
- 工程進捗、完了条件、番号付き手順、注意、Tips、成功状態、コード例を読みやすく表示する
- 一画像一操作、前へ／次へ、紹介ページ・テンプレート本体・完成サンプルへの導線を維持する
- コピーボタン以外はJavaScriptへ依存せず、JavaScript無効時も本文と指示文を読めるようにする

### 使用しないもの

- 研究室名風ヘッダーと青い正方形
- `#g-nav`、`.main-contents`、`data-accordion`、`.scroll-point`
- 研究テーマ、NEWS、メンバー等のテンプレート用セクション構造
- テンプレートのナビゲーション生成、アコーディオン、「もっと見る」JavaScript
- `assets/template/`内のCSS・JavaScript

マニュアル用資産は`assets/manual/`に配置する。

紹介ページの現在地表示やヘッダーに対する今回の調整は、マニュアル3ページのデザインへ持ち込まない。

## 3. 共通化の範囲

紹介ページとマニュアルで共通化してよいものは次の最小範囲に限る。

- サイト名称
- 相互リンク
- copyrightと権利表示
- 日本語の基本的な文章表現
- スキップリンク、フォーカス表示、見出し階層等のアクセシビリティ基準
- モバイル対応と横スクロール防止という品質基準

次は共通化を要件としない。

- headerとnavigationのHTML
- CSSクラスとCSSファイル
- JavaScript
- ページ幅
- セクション構造
- ボタン表現
- レイアウトとコンポーネント

## 4. レスポンシブとアクセシビリティ

- 紹介ページは`HPtemplate`本体のPC・スマートフォン切替を利用する
- マニュアルは42rem以下でナビゲーション、進捗、前後導線を読みやすく組み替える
- 両系統ともスキップリンク、明確なフォーカス表示、セマンティックな見出し階層を維持する
- 画像に内容に合う`alt`を付け、装飾画像は空の`alt`とする
- JavaScript無効時にも主要情報へ到達できるようにする
- `prefers-reduced-motion`時は滑らかなスクロールを無効にする

## 5. 素材と保守

テンプレート由来のCSS、JavaScript、画像、SVGは`THIRD_PARTY_NOTICES.md`とテンプレート本体の利用条件に従う。外部hotlinkや兄弟リポジトリへの相対参照は残さない。

マニュアルの操作画像は、文章だけでは対象を特定しにくいGitHub操作に限定し、既存の出典情報を維持する。
